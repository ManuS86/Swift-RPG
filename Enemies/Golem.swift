class Golem(name = "Golem", maxHp = 250.0): Enemy(name, maxHp) {
    var isTaunting = falsevar tauntTimer = 0
    
    func smash(_ target: Hero) {
        
    }
    
    func groundSlam(_ targets: [Hero]) {
        
    }
    
    func taunt() {
        
    }
}

